# Tests

Compact process and TDD documentation for `hgfloater`.

Use `docs/tests/test-index.md` as the short catalog. Put detailed process cases
under `docs/tests/cases/`. Runtime/manual plans can remain in `TESTS.md`, and C
smoke tests remain in the project test directories.
