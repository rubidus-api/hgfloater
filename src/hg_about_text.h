#ifndef HG_ABOUT_TEXT_H
#define HG_ABOUT_TEXT_H
#define HG_ABOUT_README_W L"# HGFloater\r\n" L"\r\n" L"**English** | [한국어](README.ko.md)\r\n" L"\r\n" L"**v0.17.9** — built 2026-08-30\r\n" L"\r\n" L"**[Download hgfloater.exe — v0.17.9, 630 KB](https://github.com/rubidus-api/hgfloater/releases/latest/download/hgfloater.exe)** · [All releases](https://github.com/rubidus-api/hgfloater/releases)\r\n" L"\r\n" L"HGFloater is a lightweight desktop utility for **Windows 11 and above**. A small\r\n" L"translucent widget floats on your desktop; clicking it opens a dashboard that\r\n" L"launches your shortcuts, switches between running windows, and puts volume,\r\n" L"brightness, opacity, and a command console one click away. It is written in pure\r\n" L"C against the Win32 API with zero external dependencies: the whole program is a\r\n" L"single **executable of about 630 KB** that needs no installer and no runtime, so\r\n" L"it starts instantly and stays out of your way.\r\n" L"\r\n" L"\r\n" L"---\r\n" L"\r\n" L"## Table of Contents\r\n" L"\r\n" L"1. [Overview](#1-overview)\r\n" L"2. [Install and First Run](#2-install-and-first-run)\r\n" L"3. [The Two Windows](#3-the-two-windows)\r\n" L"4. [The Floater](#4-the-floater)\r\n" L"5. [The Taskbox](#5-the-taskbox)\r\n" L"6. [The Toolbar](#6-the-toolbar)\r\n" L"7. [The Options Menu](#7-the-options-menu)\r\n" L"8. [The Command Box](#8-the-command-box)\r\n" L"9. [Monitor Thumbnails](#9-monitor-thumbnails)\r\n" L"10. [Notes](#10-notes)\r\n" L"11. [Keyboard Reference](#11-keyboard-reference)\r\n" L"12. [Mouse Reference](#12-mouse-reference)\r\n" L"13. [Configuration File](#13-configuration-file)\r\n" L"14. [Files and Directories](#14-files-and-directories)\r\n" L"15. [Building From Source](#15-building-from-source)\r\n" L"16. [Project Layout](#16-project-layout)\r\n" L"17. [Tests and Verification](#17-tests-and-verification)\r\n" L"18. [About the Developer](#18-about-the-developer)\r\n" L"19. [The HellGates Series](#19-the-hellgates-series)\r\n" L"20. [License](#20-license)\r\n" L"\r\n" L"---\r\n" L"\r\n" L"## 1. Overview\r\n" L"\r\n" L"HGFloater aims at one thing: making everyday desktop control faster than the\r\n" L"stock taskbar allows. It works as a **quick launcher**, a **task switcher**, a\r\n" L"**system control panel** (volume, per-display brightness and scaling, window\r\n" L"opacity, screen lock), a **command box** for driving windows by name and number,\r\n" L"and a **note pad** - all reachable from a widget you can park anywhere on any\r\n" L"monitor.\r\n" L"\r\n" L"Design principles worth knowing before you use it:\r\n" L"\r\n" L"- **Nothing runs in the background but the app itself.** No services, no\r\n" L"  installer. A single `hgfloater.exe`. It writes exactly one registry value, and\r\n" L"  only if you switch **Start with Windows** on - the ordinary per-user `Run`\r\n" L"  entry, which you can see and delete yourself.\r\n" L"- **Plain files on disk, in one folder.** Settings live in a plain `config.ini`\r\n" L"  under your user profile, and notes are plain `.txt` files beside it. Nothing\r\n" L"  is a database, everything is editable by hand, and the program writes no logs,\r\n" L"  caches, or temporary files.\r\n" L"- **Everything is adjustable in place.** Size, opacity, font size, grid shape,\r\n" L"  and colors change live with the wheel or the keyboard, and persist by\r\n" L"  themselves.\r\n" L"- **Keyboard and mouse are equal citizens.** Every action has both a pointer\r\n" L"  gesture and a key.\r\n" L"- **The taskbox has no background of its own.** The desktop shows through\r\n" L"  between the icons and behind the function buttons and shortcuts; only the\r\n" L"  icons, their labels and the border are drawn. Each lettered function button\r\n" L"  keeps **one white line** around it, because a button needs an edge to be a\r\n" L"  button. A side effect worth knowing: clicks land on whatever is behind those\r\n" L"  gaps, so drag the box by its `M` button rather than by the space between\r\n" L"  icons.\r\n" L"- **It follows the system theme.** Switching Windows between light and dark mode\r\n" L"  re-colors everything immediately. The **widgets** invert it on purpose - a\r\n" L"  control has to stand out against the desktop behind it - while the **document\r\n" L"  windows** (notes, note list, clipboard history, command box, About) follow it\r\n" L"  directly: light grey page with black text on a light theme, black page with\r\n" L"  white text on a dark one. Under high contrast both defer to the colors you\r\n" L"  chose there.\r\n" L"\r\n" L"## 2. Install and First Run\r\n" L"\r\n" L"1. **Download** the latest `hgfloater.exe` from the\r\n" L"   [Releases](https://github.com/rubidus-api/hgfloater/releases/tag/v0.17.2)\r\n" L"   page.\r\n" L"2. **Run it.** There is no installer. On first launch it creates\r\n" L"   `%USERPROFILE%\\.HellGates\\hgfloater\\` with a `config.ini` and a `shortcuts`\r\n" L"   folder, then shows the floater.\r\n" L"3. **Add shortcuts.** Drop `.lnk` or `.url` files into\r\n" L"   `%USERPROFILE%\\.HellGates\\hgfloater\\shortcuts`. They appear in the taskbox\r\n" L"   automatically; press `Esc` in the taskbox to re-scan the folder immediately.\r\n" L"   A shortcut that points at a **folder** goes to the\r\n" L"   [`Dir` button's list](#61-the-dir-button) rather than the icon grid.\r\n" L"4. **Summon it from anywhere** with `Win + Alt + Space` (configurable).\r\n" L"\r\n" L"Only one instance runs at a time. Launching `hgfloater.exe` again simply\r\n" L"signals the running copy instead of starting a second one.\r\n" L"\r\n" L"### Checking what you downloaded\r\n" L"\r\n" L"The executable is unsigned, so nothing but the file itself vouches for it. Three\r\n" L"ways to check, none of which require taking anyone's word:\r\n" L"\r\n" L"- **Verify the file.** Every release lists the **SHA-256** of the download.\r\n" L"  In PowerShell: `Get-FileHash .\\hgfloater.exe -Algorithm SHA256`. If it\r\n" L"  matches the release notes, you have the file that was built here.\r\n" L"- **Try the `.zip`, when a release carries one.** Some releases also offer the\r\n" L"  same executable inside a zip, which browser download-protection tends to\r\n" L"  treat less harshly.\r\n" L"- **Read the source.** All of it is in this repository under the MIT licence,\r\n" L"  and the build is reproducible from the included `Makefile` / `build.bat`.\r\n" L"\r\n" L"One thing the binary demonstrably cannot do: **reach the network**. It imports\r\n" L"no socket, HTTP, or download function of any kind, so it cannot phone home,\r\n" L"fetch anything, or send anything anywhere.\r\n" L"\r\n" L"Two features are **switched off in the published builds** until the executable\r\n" L"is code-signed — the [maximize-button menu](#41-the-maximize-button-menu), which\r\n" L"needs a system-wide mouse hook, and **Start with Windows**, which writes one\r\n" L"value under the per-user `Run` key. Both stay in the options menu, greyed, reading\r\n" L"`(off in this build)`.\r\n" L"\r\n" L"## 3. The Two Windows\r\n" L"\r\n" L"HGFloater is built from two windows that trade places:\r\n" L"\r\n" L"| Window | What it is | How you get it |\r\n" L"| :--- | :--- | :--- |\r\n" L"| **floater** | A small always-on-top widget: clock, date, host name, and system bars. | The default state. |\r\n" L"| **taskbox** | The dashboard: running windows, your shortcuts, and the toolbar. | Click the floater, or press the global hotkey. |\r\n" L"\r\n" L"Expanding centers the taskbox on the floater and then pushes it fully inside\r\n" L"that monitor's work area, so a floater parked at a screen edge never yields a\r\n" L"clipped dashboard. Collapsing returns the floater to where it was — and if you\r\n" L"moved the taskbox while it was open, the floater travels the same distance, so\r\n" L"the pair stays where you left it.\r\n" L"\r\n" L"## 4. The Floater\r\n" L"\r\n" L"The floater is the resting state: a compact translucent panel that stays on top\r\n" L"of other windows.\r\n" L"\r\n" L"**What it shows**\r\n" L"\r\n" L"- **Clock and date**, refreshed every second, sized proportionally to the\r\n" L"  widget itself.\r\n" L"- **Host name** in a thin line across the top.\r\n" L"- **Status bars** for CPU, temperature, GPU, memory, and battery: horizontal\r\n" L"  bars running behind everything - the host name included - with small labels\r\n" L"  down the left edge and each row's **reading printed down the right**. Full\r\n" L"  width means 100%; the two temperature rows use their own scale, below. A row\r\n" L"  whose value the machine does not report is absent entirely, which is why\r\n" L"  desktops have no battery row and machines without sensors have no `TMP` or\r\n" L"  `GPU`. Set `show_stats=0` in `config.ini` to hide the whole panel.\r\n" L"- **`TMP`** is drawn on a fixed **20 to 100 degree Celsius** scale rather than as\r\n" L"  a percentage, with the reading printed on its own bar, because the difference\r\n" L"  between 55 and 75 degrees matters more than a few pixels of bar length can\r\n" L"  say.\r\n" L"\r\n" L"  **`GPU`** follows it on the same scale, and it gets a better answer than the\r\n" L"  CPU does: the adapter's own sensor, read through the same interface Task\r\n" L"  Manager uses, with no vendor SDK and no driver. Plenty of drivers report\r\n" L"  nothing, and those machines simply have no GPU row.\r\n" L"\r\n" L"  The CPU reading is read from an **ACPI thermal zone** your firmware exposes. That is the\r\n" L"  only temperature reachable without installing a kernel driver and running as\r\n" L"  administrator, and it is worth knowing what it is: **a sensor on the board\r\n" L"  near the CPU, not the CPU die**. It tracks the processor without being it, and\r\n" L"  it lags under a sudden load. Many machines expose no zone at all - on those\r\n" L"  the row simply is not there, the same way the battery row is absent on a\r\n" L"  desktop. If you need true per-core readings, use a tool built around a\r\n" L"  hardware-monitoring driver; HGFloater will not become one.\r\n" L"\r\n" L"  Firmware usually declares **several** zones, and often puts one it never\r\n" L"  updates in front of the live one, so a single reading taken blindly is a coin\r\n" L"  toss. HGFloater reads **every** zone, from both surfaces Windows offers - the\r\n" L"  WMI class and the thermal performance counters, which do not always agree\r\n" L"  about being available - and prefers a zone it has watched change over one that\r\n" L"  has only ever held the same number. Type **`list sensors`** in the command box\r\n" L"  to see all of them and which one is on the bar.\r\n" L"\r\n" L"**What you can do to it**\r\n" L"\r\n" L"- **Left click** — toggles the taskbox, from anywhere on the floater. It opens\r\n" L"  in place, and the floater hides itself while the taskbox is up.\r\n" L"- **Hover** — nothing, unless **Open the Taskbox on Hover** is switched on in\r\n" L"  the [Options menu](#7-the-options-menu). With it on, resting the pointer on\r\n" L"  **the clock's minutes** — the two digits after the colon, and nothing else on\r\n" L"  the floater — opens the taskbox. The click above keeps working from anywhere\r\n" L"  either way.\r\n" L"- **Left drag** — moves the floater anywhere on any monitor. A press and\r\n" L"  release without moving is a click and toggles the taskbox; a few pixels of\r\n" L"  travel makes it a drag instead. `Alt + drag` does the same and is the one to\r\n" L"  use with **Open the Taskbox on Hover** switched on, since the dashboard opens\r\n" L"  under the pointer before an ordinary drag can start.\r\n" L"- **Alt + Wheel** — opacity.\r\n" L"- **Ctrl + Wheel** or **Ctrl + Left drag** — font size, which scales the whole\r\n" L"  widget with it.\r\n" L"- **Alt + Arrows / WASD** — moves the window from the keyboard.\r\n" L"- **`T`** — opens the taskbox. **`C`** or **`Ctrl + E`** — opens the Command\r\n" L"  Box. **`F1`** — About.\r\n" L"\r\n" L"When the cursor leaves the taskbox, it collapses back to the floater after a\r\n" L"half-second grace period, so brushing past the edge does not dismiss it.\r\n" L"\r\n" L"### 4.1 The maximize button menu\r\n" L"\r\n" L"While HGFloater is running, **right-click the maximize button** — the one left\r\n" L"of the X — on **any** window, and you get HGFloater's menu for that window:\r\n" L"**Move to (0, 0)**, the same size presets the task icons offer, and **Close**,\r\n" L"which shuts the menu without touching the window. Left-clicking the button still\r\n" L"maximizes; right-clicking a caption button does nothing in Windows, so nothing\r\n" L"was taken away.\r\n" L"\r\n" L"It works on HGFloater's own document windows too — a note, the note list, the\r\n" L"command box, the About window — and on any window whose title bar answers\r\n" L"hit-tests its own way (a PuTTY-style utility): where the window declines to\r\n" L"name its buttons, the DWM-computed button bounds say which third the click\r\n" L"landed on.\r\n" L"\r\n" L"Switch it off with **Menu on Maximize Button** in the options menu, or\r\n" L"`caption_menu=0` under `[etc]`. **Close HGFloater and the behaviour is gone** —\r\n" L"there is nothing installed and nothing to uninstall.\r\n" L"\r\n" L"Three cases where the click does what it always did, none of them a failure:\r\n" L"\r\n" L"- **Windows running as administrator.** Windows does not let an unelevated\r\n" L"  program touch input bound for an elevated one, and HGFloater runs unelevated\r\n" L"  on purpose.\r\n" L"- **Windows with no maximize button** — nothing to right-click.\r\n" L"- **Applications that report neither a maximize button nor caption-button\r\n" L"  bounds.** Rare, and they simply keep their ordinary behaviour.\r\n" L"\r\n" L"This is the only thing HGFloater does outside its own windows, and it needs a\r\n" L"system-wide mouse hook to do it. The hook reads the button and the coordinates\r\n" L"and nothing else; it holds no keyboard hook and never has. The design and its\r\n" L"costs are in `docs/RFC-2026-07-caption-button-menu.md`.\r\n" L"\r\n" L"## 5. The Taskbox\r\n" L"\r\n" L"The taskbox is a grid of icons with a status line on top and a toolbar of\r\n" L"built-in buttons.\r\n" L"\r\n" L"### 5.1 Running windows and shortcuts\r\n" L"\r\n" L"- **Task icons** come first: **one per window, not one per program**, in the\r\n" L"  order Windows reports them. A program with four windows open gets four icons,\r\n" L"  each going straight to its own window.\r\n" L"\r\n" L"  That now includes **HGFloater's own windows** — every open note editor, the\r\n" L"  note list, the clipboard history, the command box. All of them are **ordinary\r\n" L"  application windows**: each gets its own taskbar button, its own Alt-Tab\r\n" L"  entry, and all three caption buttons — minimize, maximize, close — and they\r\n" L"  stay open regardless of what the floater and taskbox do, until you\r\n" L"  close them. The floater, the taskbox and the toolbar stay out: they are the\r\n" L"  thing you are looking at.\r\n" L"- **Shortcut icons** follow: one per `.lnk` or `.url` in your shortcuts folder.\r\n" L"\r\n" L"> ### 📌 How to add a shortcut icon\r\n" L">\r\n" L"> 1. **Open the options menu** — the `Opt` button, `Ctrl + O`, or a right-click\r\n" L">    on the status line.\r\n" L"> 2. Choose **Open Shortcuts Folder**. Explorer opens on\r\n" L">    `%USERPROFILE%\\.HellGates\\hgfloater\\shortcuts\\`.\r\n" L"> 3. **Copy a shortcut into it** — any `.lnk` or `.url`. Drag one out of the\r\n" L">    Start menu, or right-click a program and *Send to → Desktop*, then move\r\n" L">    that file here.\r\n" L">\r\n" L"> The new icon appears within a second. No restart, no settings screen: the\r\n" L"> folder **is** the setting.\r\n" L">\r\n" L"> **To change the order, rename the files.** Icons are sorted by file name, so\r\n" L"> putting a number in front controls where each one sits:\r\n" L">\r\n" L"> ```\r\n" L"> 0001 Visual Studio Code.lnk\r\n" L"> 0002 Firefox.lnk\r\n" L"> 0010 Calculator.lnk\r\n" L"> ```\r\n" L">\r\n" L"> Pad the numbers to the same width — `0001`, not `1`. The sort is a plain\r\n" L"> alphabetical one, so `10` would come before `2` otherwise. Leaving gaps\r\n" L"> (`0010`, `0020`, `0030`) means you can slip something in later without\r\n" L"> renaming everything after it.\r\n" L"- **Left click** activates a task or launches a shortcut.\r\n" L"\r\n" L"**Every icon carries its key** in a small box at its top-left corner. The badge\r\n" L"is the whole chord: the `S` is `Shift`, so `S3` means **`Shift + 3`**, which\r\n" L"activates that icon exactly as clicking it would.\r\n" L"\r\n" L"- **Windows take the digits:** `S0` to `S9`, the first ten, and no key past\r\n" L"  that.\r\n" L"- **Shortcuts take the letters:** `SA` to `SZ`, in the order they sit in the\r\n" L"  grid.\r\n" L"\r\n" L"They used to share one run of labels, all of it windows, so which key opened\r\n" L"what depended on how many windows happened to be open — and a shortcut had no\r\n" L"key at all. Split, both halves hold still: `Shift + 3` is the fourth window\r\n" L"whatever else is running, and `Shift + K` is the same shortcut every time,\r\n" L"because the shortcuts folder is a list you keep. Past ten windows or\r\n" L"twenty-six shortcuts there is no badge and no key, which is honest: a label\r\n" L"nobody can press is furniture.\r\n" L"\r\n" L"`Shift` is what keeps this clear of the bare-letter grid movement (`WASD`) and\r\n" L"of the bare `C` that opens the command box.\r\n" L"\r\n" L"**Every button in that half of the grid is framed** — the function buttons and\r\n" L"the shortcuts alike — in **your Windows accent colour**, two pixels thick. The\r\n" L"taskbox is see-through, so \"the background\" is whatever you have on your\r\n" L"desktop; a one-pixel white line vanished against a light wallpaper and was hard\r\n" L"to find against a busy one. The accent is nudged toward the middle of its range\r\n" L"first, because a very pale accent disappears on white and a very dark one\r\n" L"disappears on black, and this line has to hold on both.\r\n" L"\r\n" L"**The function buttons wear the same badge**, for the same reason: `C` shows\r\n" L"`C`, `Note` shows `N`, `Clip` shows `CL`, `Opt` shows `CO`, `X` shows `CQ`. The\r\n" L"modifiers are their initials in a fixed order — `C`trl, `A`lt, `S`hift, `W`in —\r\n" L"so `CN` is `Ctrl + N` and `CSR` is `Ctrl + Shift + R`. A button no chord reaches\r\n" L"wears nothing, and a key whose name is too long for the box (`Space`, `Delete`)\r\n" L"is left to the tooltip, which spells it out in full.\r\n" L"\r\n" L"**Hover a tabbed application's icon and its tabs appear next to it.** Off by\r\n" L"default; turn it on with **Show Tabs as Task Icons** in the options menu, or\r\n" L"`show_tabs=1` under `[taskbox]` in `config.ini`. The window keeps its single\r\n" L"icon — orderable like any other — and the hover box lists its tabs by title:\r\n" L"\r\n" L"**Pointing at a task icon outlines its window on the desktop.** A frame appears\r\n" L"around the window itself, so \"which one is that?\" is answered by where it is and\r\n" L"not only by its name. Nothing else happens - the window is not raised, not\r\n" L"focused, and the outline takes no clicks, because pointing at something is not\r\n" L"choosing it. The keyboard focus does the same as the pointer. A **minimized**\r\n" L"window is left alone: there is no place on the desktop to point at, and a frame\r\n" L"around where it used to be would be a lie. A window sitting **behind** others is\r\n" L"outlined all the same - the frame says where it is, not what is in front of it.\r\n" L"\r\n" L"The box opens on hover **and** when the keyboard focus lands on the icon, so\r\n" L"both ways of getting there work the same.\r\n" L"\r\n" L"**It never covers the icon.** The box sits flush above or below it — never\r\n" L"beside it — running right from the icon's left edge or left from its right\r\n" L"edge, so the icon's whole width falls inside the box's and the two read as one\r\n" L"piece. Of those four placements it takes the first that fits the display whole,\r\n" L"which is what keeps it from running off an edge near a corner.\r\n" L"\r\n" L"These keys are the box's, and they are **the same in all three lists** — the\r\n" L"tabs, the `Dir` folders and the `Set` controls:\r\n" L"\r\n" L"| Key or click | What it does |\r\n" L"| :--- | :--- |\r\n" L"| `1`-`9` | Straight to that row, whether or not you have stepped into the box. |\r\n" L"| `0` | The last row, the same way. |\r\n" L"| `Up` / `Down` | **Step into the box**, and from then on move the selection in it. `Down` enters at the top row and `Up` at the bottom, so the selection lands where the key was already pointing. The box is what sits above or below the icon, so those keys reach it rather than stepping over it to the next row of icons. |\r\n" L"| `Tab` | Step into the box as well. On a `Dir` or `Set` button, `Space` does it too: activating the button opens its list **and** hands it the keyboard. |\r\n" L"| `Home` / `End` | The first row, the last row. The selected row is filled and marked, so where the arrows are standing is never in doubt. |\r\n" L"| `Left` / `Right` | Leave the box and move to the icon left or right — a list is a column, and sideways is what the grid means by those keys. The next icon opens its own box if it has one. **One exception:** on a `Set` row that holds a percentage, Left and Right are less and more. |\r\n" L"| `a`-`z`, `A`-`Z` | The labels past the ninth row (inside the box, because those letters move the grid outside it). |\r\n" L"| `Enter` / `Space`, or a click | Take that row: switch to the tab, open the folder, flip the switch. |\r\n" L"| A tooltip | On every `Set` row, as you arrive: which keys that row answers — the arrows for a number, `Space` or `Enter` for a switch. |\r\n" L"| Right click | Close that tab. The list stays up. (Tabs only.) |\r\n" L"| `Esc` | Leave the box. The keyboard stays in the taskbox, where it was. |\r\n" L"\r\n" L"**While you are using the keyboard, the pointer is ignored.** The taskbox\r\n" L"answers the mouse continuously — a pointer resting on an icon opens that icon's\r\n" L"list, a pointer that has wandered off closes the box and eventually collapses\r\n" L"the taskbox — and all of that is wrong the moment you reach for the arrow keys,\r\n" L"because the pointer is then sitting wherever it was left. So the first key press\r\n" L"hands control to the keyboard, and it stays there until the mouse **actually\r\n" L"moves**: `Space` on a function icon opens that icon's list no matter where the\r\n" L"mouse is, and nothing the pointer is resting on will close it.\r\n" L"\r\n" L"**A pointed-at row is drawn selected**, the same filled bar the arrows leave, so\r\n" L"the row a click would take always looks like the row a click would take.\r\n" L"\r\n" L"**A row too long for the box says the rest in a tooltip** — whether the pointer\r\n" L"found it or the arrows did. The box is measured against its own rows first and\r\n" L"takes the width they need, so most lists are read whole; the tooltip is for what\r\n" L"is longer than any box should be.\r\n" L"\r\n" L"Digits work immediately because the grid does not use them; the letters wait\r\n" L"for `Tab` because `WASD` moves the focus and `C` and `N` open other things.\r\n" L"\r\n" L"**The tabs are read when you look at them**, once per box, and never on a\r\n" L"timer — a window nobody hovers costs nothing at all.\r\n" L"\r\n" L"Nothing about this is specific to browsers. What limits it is cost: a tab is not\r\n" L"a window, so the only supported way to find one is **UI Automation** — a call\r\n" L"into the other application's own UI thread, which is slow and can block if that\r\n" L"thread is busy. Asking every window on the desktop would make that cost the cost\r\n" L"of running HGFloater, so it asks only windows whose **class** is on a list:\r\n" L"\r\n" L"| Built in | |\r\n" L"| :--- | :--- |\r\n" L"| `Chrome_WidgetWin_1` | Chrome, Edge, Brave, Opera, and Electron apps |\r\n" L"| `CabinetWClass` | File Explorer |\r\n" L"| `MozillaWindowClass` | Firefox |\r\n" L"| `CASCADIA_HOSTING_WINDOW_CLASS` | Windows Terminal |\r\n" L"| `Notepad` | Notepad |\r\n" L"\r\n" L"**Any other application can be added** with `tab_classes` under `[taskbox]`,\r\n" L"semicolon-separated, no rebuild needed:\r\n" L"\r\n" L"```ini\r\n" L"[taskbox]\r\n" L"tab_classes=SomeApp_Frame;OtherApp\r\n" L"```\r\n" L"\r\n" L"Run **`show windows class`** (`s w class`) in the command box to see the class of\r\n" L"every open window, which is where those names come from. An application whose\r\n" L"tabs UI Automation does not publish will simply keep its single icon.\r\n" L"\r\n" L"The reading happens **on a background thread**, and the box draws from what was\r\n" L"last known while the fresh answer is on its way — a cross-process call whose\r\n" L"cost grows with the browser's own accessibility data is never waited for.\r\n" L"Anything that fails falls back silently to one icon for the window. Some\r\n" L"applications do not\r\n" L"publish off-screen tabs at all, and those tabs will not appear. The design and\r\n" L"what it costs are in `docs/RFC-2026-07-tabs-as-task-icons.md`.\r\n" L"- **Left drag** on a task icon reorders it within the grid.\r\n" L"- **Right click** (or `Enter` / `F2` on the focused icon) opens its menu:\r\n" L"  - **Run (&R)** — start a new instance, or launch the shortcut.\r\n" L"  - **Focus (&F)** — switch to the existing window.\r\n" L"  - **Close Window (&X)** — task icons only. On a **tab** icon this reads\r\n" L"    **Close Tab** and closes only that tab, by invoking the tab's own close\r\n" L"    button; if the tab has no such button nothing is closed and the status line\r\n" L"    says so. **Focus** on a tab switches to that tab rather than only raising\r\n" L"    its window.\r\n" L"  - **Open File Location (&O)** — shortcut icons only.\r\n" L"\r\n" L"### 5.2 The status line\r\n" L"\r\n" L"A single-line read-only field across the top of the taskbox.\r\n" L"\r\n" L"- It shows **one message at a time** — the most recent one replaces its\r\n" L"  predecessor.\r\n" L"- Ten seconds after the last message it falls back to the **current time**,\r\n" L"  written as `2026. 11. 23.(Tue) 13:24`, and refreshes as the minute changes.\r\n" L"- **Right click** it to open the options list (the same one the `Opt` button\r\n" L"  opens).\r\n" L"- **Left drag** it to move the whole taskbox.\r\n" L"- **Ctrl + Wheel** over it changes only its own font size.\r\n" L"\r\n" L"### 5.3 Shape and size\r\n" L"\r\n" L"- **Drag any border** to change the grid: the taskbox snaps to whole columns, so\r\n" L"  no half icon is ever left hanging.\r\n" L"- **Ctrl + Wheel** (or `Ctrl` + `+` / `-`) scales icons and text together and\r\n" L"  keeps the window proportional.\r\n" L"- **Alt + Wheel** (or `Alt` + `+` / `-`) changes opacity.\r\n" L"- **`Ctrl` + `R` / `0`, or `F5`** resets position, size, and opacity.\r\n" L"\r\n" L"## 6. The Toolbar\r\n" L"\r\n" L"Ten built-in buttons sit in the same grid as the icons. Their order is fixed.\r\n" L"Each carries a short word rather than a single capital, and three of them open a\r\n" L"list instead of doing something outright.\r\n" L"\r\n" L"The word is **fitted to the button**: up to three letters it stays on one line,\r\n" L"and four to six letters are stacked on two, at a size the button can hold. The\r\n" L"alternative — shrinking a long word until it fits across — ends at a size nobody\r\n" L"can read.\r\n" L"\r\n" L"**A button's tooltip names its key.** Hovering `C` reads\r\n" L"`Command Box  (C, Ctrl+E)`; a button that no chord reaches says only what it\r\n" L"does.\r\n" L"The chords come from the [key table](#13-configuration-file) as the tooltip is\r\n" L"built, so a key you rebind is the key the tooltip shows.\r\n" L"\r\n" L"| Button | Click | Drag / Wheel |\r\n" L"| :--- | :--- | :--- |\r\n" L"| **`R`** Resize | — | **Drag** resizes the taskbox grid. |\r\n" L"| **`M`** Move | **Click** moves the taskbox aside (see below). | **Drag** moves the window. |\r\n" L"| **`X`** Exit | Quits HGFloater. | — |\r\n" L"| **`D`** Desktop | Minimizes every window; click again to restore. | — |\r\n" L"| **`C`** Command | Opens the [Command Box](#8-the-command-box). | — |\r\n" L"| **`Note`** Note | Opens the [note list](#10-notes). | — |\r\n" L"| **`Clip`** Clipboard | Opens the clipboard history, as `Ctrl + L` does; press again, with it in front, to close it. | — |\r\n" L"| **`Dir`** Folders | Opens the [folder list](#61-the-dir-button) — the shortcuts that point at a directory. | — |\r\n" L"| **`Set`** Settings | Opens the [control list](#62-the-set-button): volume, brightness, opacity, the pin, the switches. | — |\r\n" L"| **`Opt`** Options | Opens the [options menu](#7-the-options-menu), **directly under the button**. | — |\r\n" L"\r\n" L"### 6.1 The Dir button\r\n" L"\r\n" L"**Point at `Dir` — or land on it with the arrow keys, or click it — and the\r\n" L"folders appear in a list beside the button** — above it or below it, whichever fits the screen, never over the\r\n" L"button itself. Click a row, or press its number, and that folder opens in\r\n" L"Explorer; the list and the taskbox both get out of the way, because a folder is\r\n" L"somewhere you are going.\r\n" L"\r\n" L"The folders are **the shortcuts you already have**. Any `.lnk` in the shortcuts\r\n" L"folder whose target is a directory is listed here instead of taking a slot in\r\n" L"the icon grid — a folder is a place to go rather than a program to run, and a\r\n" L"dozen of them crowd out the programs. Nothing new to register: drop a folder\r\n" L"shortcut in, and it moves itself to this list. A shortcut that does not resolve\r\n" L"to a real directory stays an icon, which is what keeps anything unusual working.\r\n" L"\r\n" L"### 6.2 The Set button\r\n" L"\r\n" L"**`Set` is everything there is to set.** Point at it — or land on it with the\r\n" L"arrow keys — and the same kind of list opens. Pointing at a\r\n" L"button and arriving at it with the keyboard are the same arrival, so neither\r\n" L"needs a click first. The rows are text, so they say their whole name and what\r\n" L"turns them:\r\n" L"\r\n" L"The rows are grouped by what a row **is**, top to bottom:\r\n" L"\r\n" L"| Row | What it does |\r\n" L"| :--- | :--- |\r\n" L"| **Volume (ScrollWheel)** | The system volume, with its reading. **Wheel** over the row, or **Left / Right** once the row is selected, changes it in 5% steps; **click** mutes and unmutes. |\r\n" L"| **Brightness (ScrollWheel)** | Screen brightness, with its reading. **Wheel** or **Left / Right** changes it in 5% steps. |\r\n" L"| **Alpha (ScrollWheel)** | Taskbox opacity, with its reading. **Wheel** or **Left / Right** changes it. |\r\n" L"| **Pin** | Pins the taskbox open — while pinned, moving the mouse away no longer collapses it. **Click**, or **Space / Enter**, toggles. |\r\n" L"| **the switches** | Every on/off setting, one row each, with `on` or `off` beside it. **Click**, or **Space / Enter**, flips one. |\r\n" L"| **Settings Window...** | The [settings window](#71-the-settings-window): every option, value and key in one list. |\r\n" L"| **Edit Configuration** | Opens the config file in your editor. |\r\n" L"| **Reset Settings** | Position, size and opacity back to their defaults. |\r\n" L"| **(off in this build)** | Last: the ones this build cannot switch. Listed rather than hidden, and out of the way of the ones that work. |\r\n" L"\r\n" L"The last three came from the options menu. Everything about changing settings\r\n" L"belongs with the settings, so a reader looking for what to change finds one\r\n" L"list rather than two. They sit below the switches because a door is a bigger\r\n" L"step than a toggle, and `Reset Settings` is last of the three because it is the\r\n" L"one that throws work away.\r\n" L"\r\n" L"**A row with a number is turned; a row with a state is switched.** That is the\r\n" L"whole keyboard model for this list. On a row that holds a percentage, **Left and\r\n" L"Right are less and more** — the only place in the box where the sideways arrows\r\n" L"are not navigation, and worth the exception, because a value the wheel can turn\r\n" L"but the keyboard cannot is a value only half the readers can reach. On every\r\n" L"other row Left and Right leave the list for the neighbouring button, as they do\r\n" L"everywhere else, and **Space or Enter** does the switching. Each row says which\r\n" L"it is **in a tooltip** as you arrive on it, since nothing about the row itself\r\n" L"would tell you.\r\n" L"\r\n" L"**The switches used to be a submenu of the options menu.** They are here now,\r\n" L"beside the volume and the opacity, because they are the same kind of thing to a\r\n" L"reader — something to set, right here — and keeping the list in two places meant\r\n" L"knowing which of the two a given setting had been filed under. What stayed in\r\n" L"the options menu is what is not a setting: the shortcuts folder, the displays,\r\n" L"the audio devices, About, Reset, Exit.\r\n" L"\r\n" L"The wheel works wherever the pointer is over a row, and the reading on that row\r\n" L"follows it as it moves, so a value can be set without looking anywhere else. The\r\n" L"list stays open while you work: a control is not a destination.\r\n" L"\r\n" L"It is the same box the tabs and the folders use — same placement rules, same\r\n" L"keys, same painting — because it is the same idea: a short list belonging to the\r\n" L"button under the pointer.\r\n" L"\r\n" L"### 6.3 The Opt button\r\n" L"\r\n" L"**`Opt` opens the options as a list** — the same box `Dir` and `Set` use, in the\r\n" L"same place, answering the same keys. Click it, press `Space` on it, or press\r\n" L"`Ctrl + O`. Right-clicking the status line opens the same list.\r\n" L"\r\n" L"**Unlike `Dir` and `Set`, it does not open on hover**, and that is deliberate.\r\n" L"Those two read a list already in memory and values already cached, so opening\r\n" L"either on a passing pointer costs nothing. This one is assembled by enumerating\r\n" L"the audio endpoints through COM and asking every display for its scaling — real\r\n" L"work with real latency — and a pointer sweeping across the toolbar, or the\r\n" L"arrows walking past the button, should not set that off. It opens when it is\r\n" L"chosen.\r\n" L"\r\n" L"**Every submenu is flattened into that one list**, each row carrying the path\r\n" L"that led to it:\r\n" L"\r\n" L"```\r\n" L"Open Shortcuts Folder\r\n" L"Select Audio Device > Speakers (Realtek)\r\n" L"Select Audio Device > Mute\r\n" L"Screens > PC screen only\r\n" L"Screens > Duplicate\r\n" L"Screens > Extend\r\n" L"Screens > Second screen only\r\n" L"Display 1 > Preview Window\r\n" L"Display 1 > Scale                 125%\r\n" L"Display 1 > Brightness             50%\r\n" L"Lock Screen (Power Off)\r\n" L"About...\r\n" L"Exit\r\n" L"```\r\n" L"\r\n" L"**`Screens >` is `Win + P` without the overlay** — the four arrangements Windows\r\n" L"has, by the same call: one screen, the same picture on both, one desk across\r\n" L"both, the other screen. The one in force is ticked. They come before the\r\n" L"per-display rows because they decide which displays there are to set.\r\n" L"\r\n" L"They are **named the way Windows names them**, not \"display 1\" and \"display 2\":\r\n" L"the call is not numbered — it speaks of the internal panel and the external one,\r\n" L"which are 1 and 2 on a laptop, and on a desktop are Windows' choice. A row\r\n" L"promising a particular monitor would be promising something this cannot do.\r\n" L"\r\n" L"All four are always listed, even when one screen is attached. Hiding them would\r\n" L"be a one-way door: **PC screen only** leaves a single display, and the rows that\r\n" L"bring the other one back would be the rows that had just vanished. Windows\r\n" L"refuses an arrangement it cannot make — asking for the second screen when there\r\n" L"is no second screen — and the status line says so rather than the row appearing\r\n" L"to do nothing.\r\n" L"\r\n" L"**Scale and brightness are rows you turn**, like `Volume` in the `Set` list: the\r\n" L"**wheel** over the row, or **Left / Right** once it is selected, walks the\r\n" L"display through the values it accepts — the same 100/125/150/175/200% ladder\r\n" L"Windows offers and the same quarter steps for the backlight, one rung per notch.\r\n" L"They used to be a rung each, so one display cost eleven rows and three displays\r\n" L"cost thirty-three; the reading is on the row now and the ladder is behind it. A\r\n" L"display that will not report a value shows `--` and still steps.\r\n" L"\r\n" L"**Brightness applies as you step; scaling applies when the list closes.** A\r\n" L"backlight step is a message to the monitor and seeing it is the whole point of\r\n" L"stepping. Changing a display's scaling is not: Windows relays out every window\r\n" L"on that display, so walking from 100% to 150% while each rung applied would be\r\n" L"three reflows of the desktop to reach the one you wanted. The row moves its\r\n" L"reading as you walk it and the change is sent once — when you press `Esc`, step\r\n" L"away to another icon, click elsewhere, or the taskbox folds up. The tooltip on\r\n" L"that row says so.\r\n" L"\r\n" L"`About...` and `Exit` are last, in that order — the end of the list in the sense\r\n" L"that matters — and `About` sits above `Exit` because the two are one keystroke\r\n" L"apart and only one of them is reversible.\r\n" L"\r\n" L"A submenu that only opens when you hover its parent is a thing to discover, and\r\n" L"there is nothing here to discover: four displays' worth of brightness steps is a\r\n" L"long list, not a deep one. A ticked row keeps its `✓`, and a row that cannot be\r\n" L"used right now — a display whose driver will not report its scaling — stays in\r\n" L"place but dead, and says why in its tooltip.\r\n" L"\r\n" L"**There is no Windows menu behind this.** There was: the options were a real\r\n" L"menu with submenus, and for one version that menu was still being built, walked\r\n" L"back into rows, and thrown away every time the list opened — a tree assembled\r\n" L"only to be flattened. The rows are what gets built now, and they are built fresh\r\n" L"each time the list opens, so what it shows — the audio devices, the displays,\r\n" L"which row is ticked — is the machine as it is at that moment.\r\n" L"\r\n" L"**`M` — move aside.** Clicking the move handle without dragging nudges the pair\r\n" L"out of the way on its own, just far enough to stop covering the spot it was\r\n" L"sitting on. Clicks keep their heading, so pressing it repeatedly walks the\r\n" L"window across the screen; when that heading runs out of room it turns\r\n" L"counter-clockwise — north, west, south, east, and back to north. If no direction\r\n" L"has room, nothing moves.\r\n" L"\r\n" L"**`Bri` — brightness.** The wheel moves brightness in **5% steps**, the same as\r\n" L"opacity and volume. The `Opt` menu offers quarter steps per display when you\r\n" L"want a specific level rather than a nudge.\r\n" L"\r\n" L"HGFloater tries three things per display, in order, and remembers which one\r\n" L"answered. First the **low-level DDC/CI** path: it reads the monitor's\r\n" L"capabilities string and, if that advertises the luminance control, drives it on\r\n" L"whatever scale the monitor actually reports — which is often not 0 to 100. Then\r\n" L"the **high-level** brightness call, for monitors that support that but not the\r\n" L"first. Last a **gamma ramp**, which is worth being honest about: it dims the\r\n" L"picture, not the backlight, and costs contrast.\r\n" L"\r\n" L"A monitor can refuse all of them — DDC/CI switched off in its own menu, a cable\r\n" L"or adapter that does not carry the channel, a KVM in the path, or a monitor that\r\n" L"advertises a control it does not honour. That display shows\r\n" L"**Brightness (unavailable)** rather than accepting a click that does nothing.\r\n" L"\r\n" L"A **laptop's internal panel** is not a DDC/CI device at all, so none of that\r\n" L"would reach it. It gets its own path: HGFloater recognises the built-in display\r\n" L"by its connector and drives the real backlight through Windows' own brightness\r\n" L"service, the same one the system slider uses. That moves the lamp, not the\r\n" L"picture.\r\n" L"\r\n" L"**`Pin`** is a row of the `Set` list. While pinned, moving the mouse away no\r\n" L"longer collapses the taskbox. Explicit closes — `X`, `Esc`, the global hotkey,\r\n" L"a floater click — still work. Click it again to unpin.\r\n" L"\r\n" L"\r\n" L"### The clipboard history\r\n" L"\r\n" L"The **`Clip`** toolbar button, or **`Ctrl + L`** from the floater or the taskbox,\r\n" L"opens a window listing what you have copied, newest first. Press it again while\r\n" L"that window is in front and it closes; if it is minimized or buried, it comes\r\n" L"back instead.\r\n" L"\r\n" L"It is an **ordinary application window** — its own taskbar button, its own\r\n" L"Alt-Tab entry, minimize, maximize and close in the title bar — and it stays\r\n" L"open until you close it. Clicking elsewhere does not dismiss it, and neither\r\n" L"does taking a clip.\r\n" L"\r\n" L"Capture runs whether or not that window is open — a history that only recorded\r\n" L"while you were looking at it would not be a history — so HGFloater keeps a\r\n" L"clipboard listener alive for as long as it is running. **Text only.** Copying an\r\n" L"image or a file leaves the history untouched rather than adding an empty row.\r\n" L"\r\n" L"| What | What it does |\r\n" L"| :--- | :--- |\r\n" L"| **Click** a row, or `Enter` | Makes that clip the current one. **The window stays open**, so taking several clips in a row needs no reopening. |\r\n" L"| **Right click** a row | A menu for that clip: **Copy to Clipboard**, **Delete**, **Delete All**. The row under the pointer is selected first, so the menu always acts on what you aimed at. |\r\n" L"| `Del` | Deletes the selected clip. The selection stays where it was, so deleting several needs no re-aiming. |\r\n" L"| **Search box**, top left | Shows only the clips containing what you type, ignoring case. Clear it to see them all again. |\r\n" L"| **Number box**, top right | The most clips to keep. Default 16, set with the spin buttons, the arrow keys, or the wheel over it. |\r\n" L"| `Ctrl + Wheel` | Text size in this window, kept separate from the rest. |\r\n" L"| `Alt + Wheel` | Opacity of this window. |\r\n" L"| `Alt` + arrows | Move the window. `Ctrl` + arrows resizes it. |\r\n" L"| `Esc`, `Ctrl + W` | Closes the window. |\r\n" L"\r\n" L"**Choosing an old clip moves it to the top and pushes everything above it down\r\n" L"one.** Nothing is lost and nothing below your choice moves: the list ends up in\r\n" L"the order it would have been in had you simply copied that text again. A clip\r\n" L"identical to the one already at the top is never recorded twice, which is also\r\n" L"what keeps your own choice from coming straight back as a new entry.\r\n" L"\r\n" L"**Lowering the maximum takes effect at once** — with 20 clips kept and the\r\n" L"number set to 16, the oldest 4 are dropped as you set it, because \"at most 16\"\r\n" L"would otherwise be false the moment you asked for it. **Raising it fills\r\n" L"forward**: the dropped clips are gone, and new ones accumulate up to the new\r\n" L"number.\r\n" L"\r\n" L"**The history is bounded in bytes as well as in count.** Very large clips are\r\n" L"capped individually, and the whole history is capped in total; past the total,\r\n" L"the oldest clips go first and the newest always stays. Copying enormous text\r\n" L"blocks all day cannot make HGFloater's memory grow without limit.\r\n" L"\r\n" L"**Nothing is written to disk.** The history lives in memory and dies with the\r\n" L"program; only the maximum is saved. This is deliberate, and it is the one place\r\n" L"HGFloater does less than the clipboard managers it was measured against: a\r\n" L"clipboard history on disk is a file containing every password and recovery code\r\n" L"that passed through the clipboard, and a floating clock widget should not be the\r\n" L"program that owns that file. The cost is stated rather than hidden — **restarting\r\n" L"HGFloater empties the history.**\r\n" L"\r\n" L"---\r\n" L"\r\n" L"## 7. The Options Menu\r\n" L"\r\n" L"Open it with the [`Opt` button](#63-the-opt-button) — as a list under the\r\n" L"button, flattened to one level — with `Ctrl + O` in the taskbox, or by\r\n" L"right-clicking the status line, which opens the same list at the pointer.\r\n" L"\r\n" L"- **Settings...** — the [settings window](#71-the-settings-window): every\r\n" L"  option, every value and every key in one list.\r\n" L"- **Open Shortcuts Folder** — opens the shortcuts directory in Explorer. This is\r\n" L"  how shortcut icons are added; see [the box in 5.1](#51-running-windows-and-shortcuts).\r\n" L"- **Edit Configuration** — opens `config.ini` in Notepad.\r\n" L"- **The switches are not here any more.** Every on/off setting is a row of the\r\n" L"  [`Set` button's list](#62-the-set-button), where the volume and the opacity\r\n" L"  are: one place for the things you set. The [settings window](#71-the-settings-window)\r\n" L"  still lists them all as well, beside the numbers and the keys.\r\n" L"- **About...** — this document, rendered inside the app.\r\n" L"- **Reset Settings** — restores default geometry, opacity, sizes, and colors.\r\n" L"- **Select Audio Device** — lists the output devices with the current one\r\n" L"  checked, and offers a **Mute** toggle.\r\n" L"- **_(one entry per display)_** — every connected monitor gets its own submenu,\r\n" L"  named the way you would name it: its number, the monitor name the driver reads\r\n" L"  out of the display's EDID, and the connector it hangs off, as in\r\n" L"  `2. DELL U2720Q (DP)`. Each submenu holds everything the app can do to that\r\n" L"  one display:\r\n" L"  - **Preview Window** — turns that display's\r\n" L"    [thumbnail](#9-monitor-thumbnails) on and off.\r\n" L"  - **Scale** — the Windows display-scaling percentages (100, 125, 150, 175,\r\n" L"    200, 225). The current scale is checked; percentages the monitor cannot\r\n" L"    reach are greyed out. Choosing one changes that monitor's scale\r\n" L"    system-wide, exactly as the Settings app would.\r\n" L"  - **Brightness** — the backlight in quarter steps (0, 25, 50, 75, 100). The\r\n" L"    step nearest the monitor's current level is checked. Displays that answer\r\n" L"    DDC/CI are driven directly; the rest fall back to a gamma curve applied to\r\n" L"    that display alone, so dimming one screen leaves the others as they were.\r\n" L"\r\n" L"  The port is the socket on the graphics adapter, not the cable: a USB-C screen\r\n" L"  running DisplayPort alt mode reports as `DP` and cannot be told apart from a\r\n" L"  DisplayPort socket. `USB-C` appears when DisplayPort is tunnelled over USB4 or\r\n" L"  Thunderbolt, which is the only case Windows reports separately.\r\n" L"- **Lock Screen (Power Off)** — locks the workstation.\r\n" L"- **Exit** — quits.\r\n" L"\r\n" L"### 7.1 The settings window\r\n" L"\r\n" L"**Settings Window...** in the `Set` list, `Ctrl + ,` from the floater or the\r\n" L"taskbox, or `settings` in the command box. One list, in four parts:\r\n" L"\r\n" L"- **Options** — the switches above. `Enter` or `Space` flips the selected one.\r\n" L"- **Fonts** — three families, each opened with `Enter`, `Space` or a double\r\n" L"  click into **Windows' own font dialog**:\r\n" L"  - **Interface** — buttons, lists, the floater.\r\n" L"  - **Command box and this window** — fixed-pitch is offered first, because both\r\n" L"    are columns of text and columns want a fixed pitch. The rest of the font\r\n" L"    list is still there: someone who wants their console in a serif has a reason\r\n" L"    and it is not this program's business.\r\n" L"  - **Notes and clipboard** — prose you write at length, which is neither of the\r\n" L"    other two jobs. It was the interface family until v0.17.8 and now has its\r\n" L"    own, `[etc] doc_font_name`, defaulting to what it used before.\r\n" L"\r\n" L"  The size you pick in the dialog is applied too: the interface size, the\r\n" L"  command box size, or the note and clipboard size, whichever family you were\r\n" L"  choosing.\r\n" L"- **Values** — the numbers: opacity, text sizes, icon size, how many clipboard\r\n" L"  entries to keep, how many command lines to remember. `Left` and `Right` change\r\n" L"  the selected one, by five for a percentage and by one otherwise.\r\n" L"- **Keys** — **window, function, keys**, which is how they are stored. A heading\r\n" L"  names the window, each row under it is a function, and **each chord is its own\r\n" L"  row** beneath the function it runs. On a function row, `Enter` waits for a\r\n" L"  chord and adds it, `Del` takes every key away, and `R` restores the built-in\r\n" L"  default. On a chord row, `Del` removes **that** chord and `Enter` replaces it\r\n" L"  with the next key you press — the old one goes only once the new one is in, so\r\n" L"  `Esc` halfway through leaves it as it was.\r\n" L"\r\n" L"  While the window is waiting for a chord, nothing else in the program answers\r\n" L"  the keyboard: `F1` records `F1` instead of opening About.\r\n" L"\r\n" L"**A row that does something looks like it does something**: the actionable rows\r\n" L"are drawn bold and underlined, and the row under the pointer is inverted. The\r\n" L"arrows leave a selection and the pointer inverts what it is over — two different\r\n" L"claims, \"this is where you are\" and \"this is what you would get\" — so a click\r\n" L"lands where you expect it to.\r\n" L"\r\n" L"Nothing here has an OK button. Every change reaches `config.ini` and the running\r\n" L"program at the moment it is made, which is what the wheel and the menus have\r\n" L"always done. The window is a view of the same three tables the command box\r\n" L"writes through, so it re-reads itself whenever it comes back to the front.\r\n" L"\r\n" L"It wears the command box's colours and font: the page and the line beneath it\r\n" L"are the same two tones as that window's transcript and input box, in the same\r\n" L"fixed-pitch face. `Ctrl + Wheel` sets that shared text size — the command box\r\n" L"follows — `Alt + Wheel` sets this window's opacity, `Alt + arrows` move it, and\r\n" L"`Esc` or `Ctrl + W` closes it.\r\n" L"\r\n" L"## 8. The Command Box\r\n" L"\r\n" L"A standalone console window, opened with the `C` toolbar button or the `C` key\r\n" L"while the floater or taskbox has focus.\r\n" L"\r\n" L"**The keyboard goes to the input box** the moment the window opens, and again\r\n" L"whenever it is activated, so the first thing you type lands where you meant it.\r\n" L"**The window opens on its own key list** rather than a blank prompt — the same\r\n" L"list `help key` prints.\r\n" L"\r\n" L"| Key | What it does |\r\n" L"| :--- | :--- |\r\n" L"| `Enter` | Run what is typed. |\r\n" L"| `Shift + Enter` | A new line. Several commands run in order, top to bottom. |\r\n" L"| `Ctrl + S` | **Scroll mode**: `Up`/`Down` move the transcript a line, `Left`/`Right` a page. |\r\n" L"| `Ctrl + H` | **History mode**: the history appears as a list, numbered from `1:` at the oldest. `Up`/`Down` move the selection, `Enter` (or a double click) puts that line into the input and leaves the mode. |\r\n" L"| `Esc` | Leave the mode, or — with no mode on — close the box and go back to the taskbox. |\r\n" L"| `Ctrl + W` | Close the box and leave the taskbox alone. |\r\n" L"| `Ctrl + Space` | Jump to the input box. |\r\n" L"\r\n" L"**The arrows stay the caret's** until you ask for a mode, so selecting and\r\n" L"editing text in the input box works the way it does in every other text box on\r\n" L"the machine. A mode announces itself twice: the title bar says which one is\r\n" L"on, and an **accent-colored frame** marks the pane the arrows now belong to —\r\n" L"the transcript in scroll mode, the input in history mode. Any key the mode has\r\n" L"no use for drops the mode and is then handled normally — typing never lands in\r\n" L"a hole.\r\n" L"\r\n" L"**The input box is always exactly three lines tall** — it tracks the font, not\r\n" L"the window, so resizing the box resizes the transcript. Type or paste past\r\n" L"three lines and the input scrolls.\r\n" L"\r\n" L"The history keeps the last **64** lines by default, changed with\r\n" L"`write value history-max <n>` or the `history_max` key in `config.ini`. Running\r\n" L"the same line twice in a row stores it once. Nothing is written to disk.\r\n" L"\r\n" L"- **`Ctrl + Wheel`** changes the text size, **`Alt + Wheel`** the opacity, and a\r\n" L"  plain wheel scrolls.\r\n" L"- Long lines **wrap** instead of running off the right edge, so a transcript of\r\n" L"  window titles and help text reads without a horizontal scrollbar.\r\n" L"- The transcript keeps a bounded tail: past its budget the **oldest lines are\r\n" L"  trimmed**, which also keeps output appearing forever — an EDIT control that\r\n" L"  hits its internal limit would otherwise drop new text silently.\r\n" L"- The window keeps its own position, size, opacity, font, and font size in\r\n" L"  `config.ini`, independent of the other widgets.\r\n" L"\r\n" L"### Commands\r\n" L"\r\n" L"Every command that names a window uses the number `show` prints beside it, and\r\n" L"that is the window's place in the same list the toolbar draws. Only the commands\r\n" L"that print numbers re-read the window list: if `go` refreshed first, windows\r\n" L"could come and go between the list you read and the number you typed.\r\n" L"\r\n" L"| Command | Short | What it does |\r\n" L"| :--- | :--- | :--- |\r\n" L"| `help` | `h` | The list below, inside the box. |\r\n" L"| `help move` | `h m` | One command explained in full, with examples. |\r\n" L"| `help key` | `h k` | The keys above, not the commands. |\r\n" L"| `show` | `s` | Windows, numbered. |\r\n" L"| `show windows` | `s w` | The same list. |\r\n" L"| `show windows class` | `s w class` | The same list with each window's class, which is what `tab_classes` takes. |\r\n" L"| `show resize` | `s r` | The resize presets, numbered — 4:3 (640x480, 800x600, 1024x768, 1280x960), then 16:9, then 9:16. |\r\n" L"| `show shortcut` | `s c` | The shortcut icons, numbered. |\r\n" L"| `show note` | `s n` | Every note, numbered for the `note` command. |\r\n" L"| `show monitor` | `s m` | Every display, numbered, with its size, its place, and whether its preview is up. |\r\n" L"| `show monitor 1` | `s m 1` | Turn display 1's [preview window](#9-monitor-thumbnails) on; run it again to close it. |\r\n" L"| `show tabs` | `s t` | Every tab of every tabbed window, numbered across all of them — which is the number `go tab` takes. |\r\n" L"| `show theme` | — | What the floater is painted from right now: the light/dark and high-contrast flags, the colour scheme in use, the opacity set and the opacity the window reports, the GDI object count, and whether its fonts exist. A diagnostic for \"the widget looks faint\", so no shorthand. |\r\n" L"| `show tabsinfo` | — | What the tab reader itself is doing: per window, whether the scoped read or a full discovery answered, how many tabs and how long it took, plus totals for queued, failed, and slow asks. A diagnostic, so no shorthand. |\r\n" L"| `show sensors` | `s s` | Every temperature sensor found, numbered, and which one `TMP` and `GPU` show. |\r\n" L"| `show sensors 2` | `s s 2` | Just sensor 2, with its unit. |\r\n" L"| `show value` | `s v` | The settable values, numbered, with what each one is now. |\r\n" L"| `show option` | `s o` | The on/off options, numbered, with what each one is now. |\r\n" L"| `show key` | `s k` | Every window, function and key. `s k floater` for one window's. |\r\n" L"| `write option 1 on` | `w o 1 on` | Set an option by number or name, to `on`, `off` or `toggle`. |\r\n" L"| `bind floater notes Ctrl+Shift+N` | — | Give a function another key. `bind floater notes default` puts the built-in ones back. |\r\n" L"| `unbind taskbox clipboard Ctrl+L` | — | Take one key away. Without a chord, takes every key of that function away. |\r\n" L"| `settings` | `set` | The [settings window](#71-the-settings-window). |\r\n" L"| `go 1` | — | Focus window 1, restoring it if it is minimised. |\r\n" L"| `go tab 4` | `g t 4` | Switch to tab 4 of the `show tabs` list, whichever window is holding it. |\r\n" L"| `resize 1 1` | `r 1 1` | Resize window 1 to preset 1. |\r\n" L"| `move 1 100 100` | `m 1 100 100` | Move window 1 to 100, 100 on the display it is already on. |\r\n" L"| `move 1 100 100 2` | `m 1 100 100 2` | Move window 1 to 100, 100 on display 2. |\r\n" L"| `find windows word` | `f w word` | Windows whose title contains `word`, listed under their `show` numbers rather than renumbered. |\r\n" L"| `note` | `n` | Open the [note list](#10-notes). |\r\n" L"| `note new` | `n n` | Write a new note and open it. |\r\n" L"| `note 3` | `n 3` | Open note 3 in its own editor. An archived note opens read-only. |\r\n" L"| `note 3 archive` | `n 3 a` | File note 3 away. |\r\n" L"| `note 3 restore` | `n 3 r` | Put it back among the active notes. |\r\n" L"| `note 3 delete` | `n 3 d` | Delete note 3, to the Recycle Bin. |\r\n" L"| `find note word` | `f n word` | Notes whose title **or body** contains `word`, under their `show note` numbers. |\r\n" L"| `clipboard` | `b` | The [clipboard history](#the-clipboard-history), numbered. |\r\n" L"| `clipboard 3` | `b 3` | Make entry 3 the current clipboard, pushing the ones above it down. |\r\n" L"| `write value 1 60` | `w v 1 60` | Set value 1 to 60. The name works too, shortened: `w v bright 60`. |\r\n" L"| `config` | `c` | Open `config.ini` in Notepad. |\r\n" L"| `clear` | `cls` | Empty the transcript. The command history is untouched. |\r\n" L"\r\n" L"`X` and `Y` are measured from the target display's own top-left corner, not from\r\n" L"the virtual desktop's, so the same pair of numbers means the same place on every\r\n" L"screen. The display number is the one the options menu shows beside that\r\n" L"monitor's name. A search term may contain spaces; everything after `windows` is\r\n" L"the term.\r\n" L"\r\n" L"`help` on its own prints one line per command; **`help <command>`** prints that\r\n" L"command's arguments, what they mean, and worked examples - `help move`,\r\n" L"`h search`, and so on.\r\n" L"\r\n" L"The input field is multi-line, so pasting several lines runs them in order, each\r\n" L"echoed behind a `>` prompt.\r\n" L"\r\n" L"## 9. Monitor Thumbnails\r\n" L"\r\n" L"**Preview Window** in a display's submenu opens a live thumbnail of that\r\n" L"display.\r\n" L"\r\n" L"- **Click and drag inside a thumbnail** to drive the real monitor: mouse input\r\n" L"  is forwarded to it, so you can operate a screen you are not looking at.\r\n" L"- **Left drag the thumbnail's edit box** to move the thumbnail window.\r\n" L"- **Right click the edit box** to close it.\r\n" L"- Each thumbnail remembers its own position and size.\r\n" L"- The refresh rate follows attention: **10 frames a second while your pointer\r\n" L"  is on the thumbnail** (that is when you are driving the other monitor\r\n" L"  through it), 5 while it is merely open, and one when other windows cover it\r\n" L"  completely. Every frame is a capture of the whole source display, so a\r\n" L"  thumbnail nobody is looking at no longer costs what one being used does.\r\n" L"\r\n" L"## 10. Notes\r\n" L"\r\n" L"Open the note list with the **`Note`** toolbar button, the `N` or `Ctrl + N` key\r\n" L"in the taskbox, or the `note` command.\r\n" L"\r\n" L"**Note windows stand on their own.** The list and every open editor are\r\n" L"ordinary application windows: each has its taskbar button and Alt-Tab entry,\r\n" L"they appear as task icons in the taskbox like any other window, and collapsing\r\n" L"or closing the taskbox does not touch them — a note stays open until you close\r\n" L"it.\r\n" L"\r\n" L"Each note is a plain UTF-8 `.txt` file, so it stays readable and editable\r\n" L"outside HGFloater. **The first line is the title**; everything after it is the\r\n" L"body. Notes live in `%USERPROFILE%\\.HellGates\\hgfloater\\note\\` and are named\r\n" L"`note-<id>-YYYYMMDD.txt`, where the date is the day the note was created.\r\n" L"\r\n" L"**The list** starts with **`+Add Note`** and then shows one note per row:\r\n" L"creation date, last-modified date, and title.\r\n" L"\r\n" L"`+Add Note` is the row the list opens on, so `N` then `Enter` writes a new note\r\n" L"without a key to remember. It answers a single click, unlike the note rows, and\r\n" L"it is there even when there are no notes yet.\r\n" L"\r\n" L"**Active and archived notes are separate.** Notes you are still writing stay on\r\n" L"top; archived ones settle underneath. Headings appear once something has been\r\n" L"archived - until then the list is just notes, and a label would be noise.\r\n" L"\r\n" L"| Key | What it does |\r\n" L"| :--- | :--- |\r\n" L"| `Enter` on `+Add Note`, or one click on it | Create a note and open it. |\r\n" L"| `Enter`, double click | Open the note in its own editor window. |\r\n" L"| `Insert` | Create a note and open it, from any row. |\r\n" L"| `K` | Archive the selected note, or restore it. |\r\n" L"| `Delete` | Delete the selected note. |\r\n" L"| `Esc`, `Ctrl + W` | Close the list. |\r\n" L"\r\n" L"**Right-click a note** (or a heading) for that half of the list:\r\n" L"\r\n" L"- **Open**, **Archive** or **Restore**, and **Delete**.\r\n" L"- **Sort by** created, modified, or title, each ascending or descending, with\r\n" L"  the one in force ticked.\r\n" L"\r\n" L"**An archived note is read-only.** Archiving files a note away, and something\r\n" L"filed away has stopped being written: its editor opens read-only, and the\r\n" L"caption says so rather than leaving a window that silently ignores typing.\r\n" L"Copying and selecting still work - reading an archived note is the point of\r\n" L"keeping it - and **Restore** makes it writable again, so this is a state and not\r\n" L"a one-way door.\r\n" L"\r\n" L"Deleting is the exception: any note can be deleted, archived or not. Deleted\r\n" L"notes go to the **Recycle Bin**, so a mistaken `Delete` is recoverable where\r\n" L"Windows would normally recover it.\r\n" L"\r\n" L"The two halves sort independently: you can keep what you are writing by most\r\n" L"recently changed and your archive by title. Each half remembers its own order.\r\n" L"\r\n" L"**The editors** are separate windows, and several can be open at once. The\r\n" L"first line is the title, and both the editor's caption **and the note's row in\r\n" L"the list** follow it as it is typed — the list does not wait for the editor to\r\n" L"close. `Esc` or `Ctrl + W` closes\r\n" L"an editor, and `Ctrl + W` closes the list the same way.\r\n" L"\r\n" L"**Right-click inside an editor** for the text and for the note itself: **Undo**,\r\n" L"**Redo**, **Cut**, **Copy**, **Paste**, **Delete** (the selection), **Select All**, then\r\n" L"**Archive** or **Restore**, and **Delete Note and Close**. Entries that would do\r\n" L"nothing are greyed, so the menu says what is possible rather than failing\r\n" L"quietly. It replaces the edit control's stock menu, which knows about the text\r\n" L"but nothing about the note it belongs to.\r\n" L"\r\n" L"Undo goes back a hundred steps and **`Ctrl + Y`** or **Redo** comes forward\r\n" L"again, because the editor hosts a rich edit control rather than the plain one,\r\n" L"whose single undo level only toggles. If that control cannot be loaded the\r\n" L"editor still opens on the plain one, with Redo greyed out.\r\n" L"\r\n" L"**`Ctrl + Wheel`** over the list or over any editor changes the note text size.\r\n" L"There is one size for all of them, so the list and every open editor follow the\r\n" L"same wheel. It is stored unscaled, so a change made on a 200% display reads the\r\n" L"same on a 100% one, and a note dragged between monitors is redrawn at that\r\n" L"display's scale. A plain wheel still scrolls.\r\n" L"\r\n" L"Changing the size **re-fits what is on screen to the window you already have**:\r\n" L"the list re-measures its rows and scrolls the selected one back into view, and\r\n" L"every open editor re-wraps its text and keeps the caret on screen. No window\r\n" L"moves or changes size - only how much of it you can read at once does.\r\n" L"\r\n" L"**Saving** happens on its own. Edits are held in memory and written a couple of\r\n" L"seconds after the typing settles, and only for the notes that actually changed,\r\n" L"so holding a key down does not turn into one write per character. Closing an\r\n" L"editor and quitting the app both flush whatever is still pending.\r\n" L"\r\n" L"Everything a text file cannot carry lives in `note\\note.ini` beside the notes:\r\n" L"which half of the list each note sits in, how each half is sorted, the shared\r\n" L"text size, the size and position of the list window, and **where each note's own\r\n" L"editor window was last left** - so a note reopens on the monitor and at the size you left it. The\r\n" L"creation time of day goes there too, since the file name only carries the day.\r\n" L"\r\n" L"## 11. Keyboard Reference\r\n" L"\r\n" L"### Global\r\n" L"\r\n" L"| Key | Action |\r\n" L"| :--- | :--- |\r\n" L"| `Win + Alt + Space` | Show or hide the taskbox. If a window has drifted off screen, it is pulled back into the nearest monitor's work area. The taskbox flashes its outline three times when a key summons it — a click does not, since the pointer is already there. |\r\n" L"| `Ctrl + ,` | The [settings window](#71-the-settings-window) — options, values, and every key on this page. |\r\n" L"\r\n" L"The keys that run a **function** can be changed, in six windows: `system` (the\r\n" L"one Windows itself carries), `widget` (the floater and taskbox together),\r\n" L"`floater`, `taskbox`, `commandbox`, `note` (the list) and `clipboard`. They are\r\n" L"kept as window, function and chords, and the settings window, `bind`, or\r\n" L"`config.ini` edits them.\r\n" L"\r\n" L"Two kinds of key are deliberately not on that list. **Navigation** — the arrows,\r\n" L"`WASD`, the grid keys, the `Shift +` letter badges — is the taskbox's own\r\n" L"language rather than a function. And **editing** — everything inside a note\r\n" L"editor, `Ctrl + X/C/V`, `Ctrl + Z/Y`, and the `Esc` that leaves a command-box\r\n" L"mode — belongs to the text under the caret; `Esc` in the command box means\r\n" L"\"leave the mode, or else go back to the taskbox\", which is a state rather than\r\n" L"a chord.\r\n" L"\r\n" L"### Inside the taskbox\r\n" L"\r\n" L"| Key | Action |\r\n" L"| :--- | :--- |\r\n" L"| `Arrow keys` / `WASD` | Move focus between icons |\r\n" L"| `Space` | Activate the focused item — a window comes forward and the dashboard collapses back to the floater, exactly as clicking it does |\r\n" L"| `Enter` / `F2` | Open the focused item's context menu |\r\n" L"| `Tab` | With a box open (tabs, `Dir`, `Set`), step into it |\r\n" L"| `Up` / `Down` | On an icon with a box: step into it. Inside one: move the selection |\r\n" L"| `Left` / `Right` | Inside a box: leave it, and move to the icon beside |\r\n" L"| `Esc` | Inside a box: leave it |\r\n" L"| `C`, `Ctrl + E` | Open the Command Box |\r\n" L"| `N`, `Ctrl + N` | Open the note list |\r\n" L"| `Ctrl + L` | Open the clipboard history |\r\n" L"| `Ctrl + O` | The options menu, as if `Opt` had been chosen |\r\n" L"| `Esc` | Hide the taskbox and re-scan shortcuts |\r\n" L"\r\n" L"### Inside the note list\r\n" L"\r\n" L"| Key | Action |\r\n" L"| :--- | :--- |\r\n" L"| `Enter` on `+Add Note` | Create a note and open it |\r\n" L"| `Enter` on a note | Open it in its own editor |\r\n" L"| `Insert` | Create a note, from any row |\r\n" L"| `K` | Archive the selected note, or restore it |\r\n" L"| `Delete` | Delete the selected note (to the Recycle Bin) |\r\n" L"| `Esc`, `Ctrl + W` | Close the list |\r\n" L"\r\n" L"### Inside a note editor\r\n" L"\r\n" L"| Key | Action |\r\n" L"| :--- | :--- |\r\n" L"| `Ctrl + Z` / `Ctrl + Y` | Undo / redo, a hundred levels deep |\r\n" L"| `Ctrl + Wheel` | Note text size, shared with the list and every other editor |\r\n" L"| `Esc`, `Ctrl + W` | Close the editor (pending edits are written first) |\r\n" L"\r\n" L"### Window manipulation (focused or hovered floater/taskbox)\r\n" L"\r\n" L"| Key | Action |\r\n" L"| :--- | :--- |\r\n" L"| `Alt` + `Arrow keys` / `WASD` | Move the window |\r\n" L"| `Ctrl` + `+` / `-` | Font and icon size |\r\n" L"| `Alt` + `+` / `-` | Opacity |\r\n" L"| `Ctrl` + `R` / `0`, `F5` | Reset position, size, and opacity |\r\n" L"\r\n" L"### System\r\n" L"\r\n" L"| Key | Action |\r\n" L"| :--- | :--- |\r\n" L"| `F1` | About |\r\n" L"| `Ctrl + W` | Close the focused document window (note, note list, clipboard history, command box, About) |\r\n" L"| `T` | Open the taskbox (from the floater) |\r\n" L"| `Ctrl + Q` | Quit. Works from any HGFloater window. |\r\n" L"| `Alt + F4` | Quits from the floater or the taskbox; closes just that window from a note, the note list, the clipboard history, the command box or About. |\r\n" L"| `Enter` | Execute (inside the Command Box) |\r\n" L"| `Shift + Enter` | New line (inside the Command Box) |\r\n" L"| `Ctrl + S` / `Ctrl + H` | Scroll mode / history mode (inside the Command Box) |\r\n" L"| `Shift` + `0`–`9` | Activate the window with that badge (`S0` … `S9`) |\r\n" L"| `Shift` + `A`–`Z` | Launch the shortcut with that badge (`SA` … `SZ`) |\r\n" L"| `Ctrl + Space` | Focus the input (inside the Command Box) |\r\n" L"| `Ctrl + Wheel` | Text size (inside the Command Box) |\r\n" L"\r\n" L"The **document windows** - notes, the note list, the clipboard history, the\r\n" L"command box, About - keep the standard Windows editing keys for themselves:\r\n" L"`Ctrl + X` cuts, `Ctrl + C` copies, `Ctrl + V` pastes, `Ctrl + Z` and `Ctrl + Y`\r\n" L"undo and redo, `Ctrl + A` selects all. Only `Ctrl + Q` and `F1` reach past them\r\n" L"to the program, so nothing you press while typing resets a setting or resizes a\r\n" L"widget.\r\n" L"\r\n" L"**`Ctrl + W` closes any of them**, the way it closes a tab or a document\r\n" L"elsewhere. In the command box it differs from `Esc`, which closes the box and\r\n" L"returns to the taskbox; `Ctrl + W` just closes.\r\n" L"\r\n" L"## 12. Mouse Reference\r\n" L"\r\n" L"| Action | Gesture |\r\n" L"| :--- | :--- |\r\n" L"| **Show / hide the taskbox** | Left-click the floater, or press the global hotkey |\r\n" L"| **Activate an item** | Left-click an icon |\r\n" L"| **Reorder icons** | Left-drag a task icon |\r\n" L"| **Item context menu** | Right-click an icon |\r\n" L"| **Options menu** | Left-click `O`, or right-click the status line |\r\n" L"| **Open a folder** | Point at `Dir`, then click a row |\r\n" L"| **Open the control list** | Point at `Set` |\r\n" L"| **Move a window** | Left-drag empty space, the status line, or the `M` button |\r\n" L"| **Move the floater** | Drag the floater, or `Alt + drag` it |\r\n" L"| **Move the taskbox aside** | Left-click the `M` button |\r\n" L"| **Resize the taskbox grid** | Drag a border, or drag the `R` button |\r\n" L"| **Font / icon size** | `Ctrl` + wheel |\r\n" L"| **Opacity** | `Alt` + wheel, or wheel over the `Alp` row of the `Set` list |\r\n" L"| **Screen brightness** | Wheel over the `Bri` row of the `Set` list |\r\n" L"| **Volume / mute** | Wheel over the `Vol` row of the `Set` list / click that row |\r\n" L"| **Pin the taskbox** | Click the `Pin` row of the `Set` list |\r\n" L"| **Remote monitor control** | Click or drag inside a monitor thumbnail |\r\n" L"| **Open the notes** | Left-click `Note` |\r\n" L"| **New note** | Left-click `+Add Note` (a single click, unlike a note row) |\r\n" L"| **Open a note** | Double-click it |\r\n" L"| **Sort, archive, or delete a note** | Right-click it, or right-click a section heading |\r\n" L"| **Note text size** | `Ctrl` + wheel over the list or an editor |\r\n" L"| **Edit a note's text** | Right-click inside an editor |\r\n" L"| **Command box text size** | `Ctrl` + wheel |\r\n" L"| **Quit** | Left-click `X`, or Exit in the options menu |\r\n" L"\r\n" L"## 13. Configuration File\r\n" L"\r\n" L"`%USERPROFILE%\\.HellGates\\hgfloater\\config.ini`, plain INI, safe to edit by hand\r\n" L"while the program is closed. Missing keys are written back with their defaults on\r\n" L"startup, so deleting a key restores it.\r\n" L"\r\n" L"Settings that change in bursts — opacity, font and icon size, window position —\r\n" L"are written once the change settles (about a second) or at exit, rather than on\r\n" L"every wheel notch.\r\n" L"\r\n" L"### `[floater]` and `[taskbox]`\r\n" L"\r\n" L"| Key | Meaning |\r\n" L"| :--- | :--- |\r\n" L"| `x`, `y` | Top-left screen coordinates |\r\n" L"| `w`, `h` | Width and height |\r\n" L"| `alpha` | Opacity, 76–255 |\r\n" L"| `font_size` | Text size |\r\n" L"| `icon_size` | Icon resolution (`[taskbox]` only) |\r\n" L"| `show_stats` | `0` hides the CPU/memory/battery line (`[floater]` only, default `1`) |\r\n" L"| `show_tabs` | `1` gives a tabbed application's tabs their own task icons (`[taskbox]` only, default `0`) |\r\n" L"| `tab_classes` | Extra window classes to look for tabs in, `;`-separated (`[taskbox]` only) |\r\n" L"\r\n" L"### `[clipboard]`\r\n" L"\r\n" L"| Key | Meaning |\r\n" L"| :--- | :--- |\r\n" L"| `max` | The most clips the history keeps, 1–64 (default `16`) |\r\n" L"\r\n" L"Only the number lives here. The clips themselves are never written to disk.\r\n" L"\r\n" L"### `[commandbox]`\r\n" L"\r\n" L"Its own `x`, `y`, `w`, `h`, `alpha`, `font_size`, and `font_name`, plus:\r\n" L"\r\n" L"| Key | Meaning |\r\n" L"| :--- | :--- |\r\n" L"| `history_max` | How many command lines `Shift + Left/Right` walk back through, 1–256 (default `64`) |\r\n" L"\r\n" L"Both this and `[clipboard] max` have no control anywhere in the interface, so\r\n" L"HGFloater writes them into `config.ini` on first run with a comment block\r\n" L"explaining them. The lines and the clips themselves are never written to disk.\r\n" L"\r\n" L"### `[etc]`\r\n" L"\r\n" L"| Key | Meaning |\r\n" L"| :--- | :--- |\r\n" L"| `font_name` | Font for edit controls, tooltips, and the About dialog (default `Segoe UI`) |\r\n" L"| `caption_menu` | `0` stops right-clicking any window's maximize button from opening HGFloater's menu (default `1`) |\r\n" L"\r\n" L"### `[colors]`\r\n" L"\r\n" L"Every accent color as `RRGGBB` hex, for example `FFD228`:\r\n" L"\r\n" L"- `scheme_bg`, `scheme_border`, `scheme_text`, `scheme_flash`, `scheme_selected`\r\n" L"  — the dark palette. These are the **widget** colors: the floater, the taskbox\r\n" L"  and the toolbar. The document windows take their page colors from the system\r\n" L"  theme instead and have no keys here.\r\n" L"- `focus_bg` — the keyboard/mouse focus highlight.\r\n" L"- `stat_cpu`, `stat_temp`, `stat_gpu`, `stat_mem`, `stat_bat` — the floater's status bars.\r\n" L"- `value_alpha_low` / `value_alpha_high`, `value_brightness_low` /\r\n" L"  `value_brightness_high`, `value_volume_low` / `value_volume_high` — the\r\n" L"  gradients behind the `Alp`, `Bri`, and `Vol` rows of the `Set` list.\r\n" L"\r\n" L"### `[keys.system]`, `[keys.widget]`, `[keys.floater]`, `[keys.taskbox]`, `[keys.commandbox]`, `[keys.note]`, `[keys.clipboard]`\r\n" L"\r\n" L"One section per window, one line per function, and the value is the chords that\r\n" L"reach it — up to four, separated by commas:\r\n" L"\r\n" L"```ini\r\n" L"[keys.floater]\r\n" L"commandbox = C, Ctrl+E\r\n" L"notes = Ctrl+N\r\n" L"clipboard =\r\n" L"```\r\n" L"\r\n" L"A line that is **absent** means the built-in default. A line that is **present\r\n" L"and empty** means the function has no key at all, which is a supported answer:\r\n" L"one reachable by button and menu does not have to own a chord as well.\r\n" L"\r\n" L"Chords are written the way they are read — `Ctrl+N`, `Alt+F4`, `Ctrl+Shift+R`,\r\n" L"`F2`, `Esc`, `Space`, `Plus`, `NumMinus` — and `Win+` belongs to `[keys.system]`\r\n" L"alone, because the shell takes it before any other window sees it. `show key`\r\n" L"lists every function, and the [settings window](#71-the-settings-window) edits\r\n" L"them without your having to spell anything.\r\n" L"\r\n" L"The older `[hotkeys]` pair of numbers is read once, so a global hotkey chosen\r\n" L"before v0.14.0 carries over into `[keys.system]`, and is then no longer used.\r\n" L"\r\n" L"### `[note]`\r\n" L"\r\n" L"| Key | Meaning |\r\n" L"| :--- | :--- |\r\n" L"| `font_size` | Note text size, in points, shared by the list and every editor |\r\n" L"| `sort_active`, `sort_archived` | Which column each half of the list is sorted by |\r\n" L"| `sort_active_desc`, `sort_archived_desc` | `1` for newest or Z first |\r\n" L"| `list_x`, `list_y`, `list_w`, `list_h` | Where the note list window sits |\r\n" L"\r\n" L"Notes themselves keep a `note/note.ini` beside their `.txt` files, but only for\r\n" L"what is about a note rather than about the program: whether it is archived, when\r\n" L"it was created, and where its own editor window was. That file travels with the\r\n" L"notes folder; the settings above belong where the other settings are.\r\n" L"\r\n" L"### `[settings]`\r\n" L"\r\n" L"| Key | Meaning |\r\n" L"| :--- | :--- |\r\n" L"| `alpha` | Opacity of the settings window, `32`–`255`. Default `255` |\r\n" L"\r\n" L"Its text size is not here: the settings window uses the command box's font and\r\n" L"size, so `Ctrl + Wheel` in either one moves both.\r\n" L"\r\n" L"## 14. Files and Directories\r\n" L"\r\n" L"| Path | Purpose |\r\n" L"| :--- | :--- |\r\n" L"| `%USERPROFILE%\\.HellGates\\hgfloater\\` | Base directory, created on first run |\r\n" L"| `%USERPROFILE%\\.HellGates\\hgfloater\\config.ini` | Every setting |\r\n" L"| `%USERPROFILE%\\.HellGates\\hgfloater\\shortcuts\\` | Your `.lnk` and `.url` files |\r\n" L"| `%USERPROFILE%\\.HellGates\\hgfloater\\note\\` | One `.txt` per [note](#10-notes), plus `note.ini` for everything the text files cannot carry |\r\n" L"\r\n" L"That is the complete list on disk. The only thing written outside it is the\r\n" L"`Run` registry value behind [Start with Windows](#7-the-options-menu), and only\r\n" L"while that is switched on. HGFloater writes no log files, no caches, and no\r\n" L"temporary files, and nothing it writes grows without bound.\r\n" L"\r\n" L"\r\n" L"\r\n" L"\r\n" L"## 18. About the Developer\r\n" L"\r\n" L"- **Author**: rubidus-api (rubidus@gmail.com)\r\n" L"- **Method**: developed with AI assistance, in the \"vibe coding\" style.\r\n" L"- **Note**: the author is a hobbyist, not a career programmer. This project is\r\n" L"  the result of creative experimentation and collaboration with AI tools.\r\n" L"\r\n" L"## 19. The HellGates Series\r\n" L"\r\n" L"\"HellGates\" is a playful parody of Bill Gates and Windows: a collection of\r\n" L"utilities meant to make the desktop lighter and more responsive. The series\r\n" L"started from wishing Windows were friendlier, quicker, and less in the way, and\r\n" L"it exists to try UX ideas that the stock shell will not.\r\n" L"\r\n" L"## 20. License\r\n" L"\r\n" L"Released under the MIT License. See [LICENSE](LICENSE).\r\n" L"\r\n"
#endif